import type { ColorTheme, FontTheme } from '../types'

export const COLOR_THEMES: ColorTheme[] = ['light', 'dark', 'system']

export const FONT_THEMES: FontTheme[] = ['sans', 'serif', 'mono']

export const DEFAULT_COLOR_THEME: ColorTheme = 'system'

export const DEFAULT_FONT_THEME: FontTheme = 'sans'
